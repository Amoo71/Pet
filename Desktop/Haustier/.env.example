# Required for real online multiplayer persistence on Vercel.
# Use Vercel KV values if available:
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Upstash Redis names also work if your integration exposes those instead:
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
